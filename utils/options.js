// Не совсем понял, должен ли был я откуда-то получать этот глубоковложенный объект
// Поэтому сделал его самостоятельно 
const options = {
  params: {
    fields: {
      isDynamic: true,
    },
  },
};

export default options;
